var currentAge=20;
var maxAge=110;
var foodPerDay=3;
var totalNeeded = (foodPerDay * 365) * (maxAge - currentAge);
console.log('You will need ' + totalNeeded + ' foods to last you until the ripe old age of ' + maxAge);