function calcCircumfrence(radius){
    circonference= Math.PI*2*radius;
    console.log('The circumference is :'+circonference);
}
function area(radius){
    area= Math.PI * radius*radius;
    console.log('The area is:'+area );
}
calcCircumfrence(6);
area(4);